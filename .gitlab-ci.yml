include: 'https://gitlab.gnome.org/GNOME/citemplates/raw/master/flatpak/flatpak_ci_initiative.yml'

flatpak:
  extends: .flatpak
  timeout: 90m
  variables:
    GIT_SUBMODULE_STRATEGY: normal

    # Replace with your manifest path
    MANIFEST_PATH: "build-aux/flatpak/org.gnome.Builder.Devel.json"
    RUNTIME_REPO: "https://nightly.gnome.org/gnome-nightly.flatpakrepo"
    # Replace with your application name, as written in the manifest
    FLATPAK_MODULE: "gnome-builder"
    APP_ID: "org.gnome.Builder.Devel"
    BUNDLE: "gnome-builder-git.flatpak"
  before_script:
    - flatpak --user install -y org.freedesktop.Sdk.Extension.llvm14//22.08beta
    - flatpak --user install -y --from https://dl.flathub.org/build-repo/97448/org.freedesktop.Sdk.Extension.node18.flatpakref
    # - flatpak --user install -y org.freedesktop.Sdk.Extension.node18//22.08

nightly:
  extends: '.publish_nightly'
  needs:
    - 'flatpak'
